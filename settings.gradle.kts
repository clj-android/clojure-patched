// Project name must be "clojure" (not "clojure-patched") so that Gradle
// composite build substitution matches the Maven coordinate
// com.goodanser.clj-android:clojure:1.12.0-1 used by the plugin.
rootProject.name = "clojure"
